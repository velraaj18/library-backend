// taskEnums.ts

export enum TaskStatus {
  Pending = 'pending',
  InProgress = 'in-progress',
  Completed = 'completed',
  New = 'new',
  RoadBlocked = 'road blocked'
}

export enum TaskPriority {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
}

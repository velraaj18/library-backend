// taskEnums.ts

export enum TaskStatus {
  Pending = 'pending',
  InProgress = 'in-progress',
  Completed = 'completed',
}

export enum TaskPriority {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
}
